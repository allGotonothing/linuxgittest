cmake_minimum_required(VERSION 3.0)

project(learn_lib)

aux_source_directory(${PROJECT_BINARY_DIR}/../src1 LIB_SRC)

include_directories(${PROJECT_BINARY_DIR}/../inc)

add_library(func_share SHARED ${LIB_SRC})

add_library(func_static STATIC ${LIB_SRC})

set_target_properties(func_share PROPERTIES OUTPUT_NAME "myfunc")

set_target_properties(func_static PROPERTIES OUTPUT_NAME "myfunc")

set(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/../lib)

